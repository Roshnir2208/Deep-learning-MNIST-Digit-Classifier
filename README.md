# Deep-learning-MNIST-Digit-Classifier
Utilizing the MNIST dataset, we benchmark the performance of three modern learning algorithms: SVM, GLVQ, and a CNN. The CNN model, implemented via MatConvNet, is highlighted for its superior ability to perform automatic feature extraction and achieve high accuracy with efficient CPU-based training, resulting in reduced computation time.
